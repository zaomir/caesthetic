# Gemini instructions — CAESTHETIC

Before any repository or project routing, read `AGENTS.md` and
`docs/projects/caesthetic/GROWTH_SCORE_AGENT_ENFORCEMENT.md` completely.

Minimal trigger: after case/dash/spacing normalization, any whole Russian or
English synonym in the enforcement adapter, standalone English `audit` form or
ordinary Russian `аудит` form routes to `growth_score_audit`. If there is no
active interview, the first sentence must be exactly:
`Вы создаёте новый аудит? Ответьте на вопросы.` Then run the mandatory Manager
Interview. Use public/open sources only. Full research is blocked until a named
manager approves the versioned Research Alignment Card. On conflict or missing
gate, fail closed with `BLOCKED`; never create an ad-hoc audit or weaken the
canonical SSOT/template/pipeline.
