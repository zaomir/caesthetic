---
owner: CAESTHETIC / Engineering
status: review_preview_in_implementation
project: caesthetic
updated: 2026-09-05
---
# Spoken v3 — route QA manifest

WEBSITE_STUDIO_STANDARD and IMPECCABLE_WEBSITE_AGENT_STANDARD apply. SURFACE MODE:
read/operate. DESIGN DISCOVERY: owner-approved v3 plan, exact supplied images,
general-to-specific narrative, two locales and explicit preservation of v1/v2.
No new customer facts or commercial results are inferred from a design task.

REPRESENTATIVE SURFACE: this paired v3 route; nine semantic sections plus intro;
review preview, not a completed new audit. Real report snapshots are unchanged.
Approved evidence and sources remain available; draft matrix results are neutral.

IMPECCABLE PASS: targeted clarify/typeset/adapt/polish checks are required through
computed styles, keyboard, source disclosure, no-JS, image hashes and manual
screenshots. No claim of tool execution is made merely by naming the pass.
DETECT TARGET: v3 HTML/CSS; existing machine design detector rejects unregistered
colors, tokens, small essential type, gradients and spacing. No baseline waiver.

QA: `node --test tests/caesthetic/spoken-medspa-v3.test.mjs`; deterministic paired
build; `node scripts/caesthetic/design-gate.mjs`; versioned browser script across
Chromium/Firefox/WebKit, 320–1440px. Submission tests are intercepted, no real
leads/payments. Production mode verifies exact public bytes, protected English
content boundaries and read-only rendering. Acceptance status comes from private
workflow artifacts and the project status entry, not this manifest's existence.

Images are original owner-approved PNG pairs, lazy below fold and with dimensions.
Evidence inputs/approval metadata/raw captures are not public payloads. Noindex
is not auth. Unlisted RU and protected EN follow existing parent scope. Rollback
removes only v3 and its task-scoped changes through canonical deployment.
